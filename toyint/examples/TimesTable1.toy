; simple loop count 1 to 12 to display a times table.

12 !num
1 !count
0 !table

"$Enter a number between 1 and 12 : " Print Read !Table

@LineBreak Print

loop:
  &count Print "$ * " Print
  &table Print "$ = " Print
  &count &table * Println
  &count 1 + !count
  &count &num <=
  jnz loop
 Exit
